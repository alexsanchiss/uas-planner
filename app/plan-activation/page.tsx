"use client";

export default function TrajectoryGenerator() {
  return (
    <div className="flex-1 flex flex-col items-center justify-center w-full bg-[var(--bg-primary)]">
      <div className="max-w-xl bg-[var(--surface-primary)] rounded-lg shadow p-8 text-center border border-[var(--border-primary)]">
        <h2 className="text-2xl font-semibold text-[var(--text-primary)] mb-4">Plan Activation</h2>
        <p className="text-lg text-[var(--text-secondary)] mb-2">We are working on this feature.</p>
        <p className="text-[var(--text-tertiary)]">Please check back soon for updates!</p>
      </div>
    </div>
  );
}
