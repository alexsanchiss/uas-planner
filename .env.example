# =============================================================================
# UAS Planner - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# MySQL connection string for Prisma
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://USER:PASSWORD@XXX.XX.XXX.XXX:3306/upps"

# -----------------------------------------------------------------------------
# AUTHENTICATION (REQUIRED)
# -----------------------------------------------------------------------------
# JWT Secret for authentication tokens
# Generate a secure random string: openssl rand -base64 32
# SECURITY: Never commit the real secret to version control
JWT_SECRET="your-secure-random-secret-here"

# -----------------------------------------------------------------------------
# EXTERNAL SERVICES (OPTIONAL)
# -----------------------------------------------------------------------------
# FAS (Flight Authorization Service) API URL
# Used for U-Plan submission and authorization
# Default: http://158.42.167.56:8000/uplan
FAS_API_URL="http://158.42.167.56:8000/uplan"

# -----------------------------------------------------------------------------
# FEATURE FLAGS (OPTIONAL)
# -----------------------------------------------------------------------------
# Production Mode flag for FlightPlansUploader component
# When "true", uses the production version with restricted bulk operations
# When "false" or unset, uses the development version with full bulk operations
# Default: false (development mode)
NEXT_PUBLIC_PRODUCTION_MODE="false"